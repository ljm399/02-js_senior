# 一.原型

## 1.1. 对象的原型

- 任何对象都有自己的默认原型(函数既有显示原型也有隐式原型)

- 作用:在当前对象找不到属性,去隐式原型找

- 获取原型(使用有兼容问题)

  - ~~~javas
    _proto_(xx._proto)
    ~~~

  - ~~~javas
    Object.getPrototypeof(obj)
    ~~~

    

## 1.2 函数的原型

- 任何函数(非箭头函数)都有自己的prototype属性(函数的显示原型),函数是对象有隐式对象

- 获取

  - ~~~javas
    prototype
    ~~~

- 作用:

  - 当通过new操作符调用函数(new xx()),会创建一个新的函数

  - 这个函数的隐式函数会指向这个函数的显示原型

  - ~~~javas
    obj._proto_ = F.prototype
    ~~~

## 1.3 构造函数的正确写法

~~~JavaScript
function Person(name, age) {
    this.name = name
    this.age = age(当通过new xx()调用时,this指向新对象,否则就window)
}
Person.prototype.running = function() {}
提醒:不是Person本身,同时放在其原型上面可以减少多次创建新对象的多个running占用内存

var p1 = new Person()
p1.running()
~~~

## 1.4 显式原型上的constructor指向函数对象本身

注意:这里不代表函数本身可以使用其显式原型的属性

## 1.5 重写显式原型

~~~JavaScript
Person.prototype = {
    
}
//注意: constructor也要写
~~~

# 二 ES5中的继承

## 2.1 面向对象的三大特征

- 抽象
- 封装
- 继承
- 多态

## 2.2 原型链的概念

- 原型对象和原型对象之间可以形成链条

## 2.3 原型实现继承

~~~JavaScript
function Person() {}
function Student() {}
Student.prototype = new Person()
//其背后操作:Student.prototype._proto_等于这个新对象
~~~

## 2.4 借用构造函数

~~~JavaScript
function Student() {
    Person.call(this,参数)//核心
}
~~~

## 2.5 原型式/寄生式思想(了解)

## 2.6 寄生组合式继承

~~~JavaScript
function inherit(Subtype, Supertype) {
    Subtype.prototype = createObject(Supertype.prototype)
    ////其背后操作(和上面一样):Subtype.prototype._proto_等于这个对象的显示原型
}
function Person() {}
function Student() {}
inherit(Student, Person)

//当Subtype.prototype = createObject(Supertype.prototype)有兼容问题
//则这步改为
function createObject(o) {
    function F() {}
    F.prototype = o
    return new F()
}
function inherit(Subtype, Supertype) {
    Subtype.prototype = createObject(Supertype.prototype)
    //到这的几步创建了个新函数,使其隐式原型等于(不是指向)Supertype.prototype,即Subtype本来的显式原型上的属性如constructor没有了
    Object.derfeneProperty(Subtype.prototype, 'constructor', {
        ennumerable: false,(控制台看不见)
        configurable: true,
        writable:true,
        value:Subtype
    })//这步作用:使Subtype.prototye有constructor
}
~~~

## 2.7 object是所有类的父类

## 2.8 对象判断方法补充(xx.方法)

- hasOwnProperty
- in/for in
- instanceOf
- obj.isPrototypeOf(info)