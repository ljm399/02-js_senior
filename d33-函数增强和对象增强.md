# 一.函数增强

## 1.1. 函数对象的属性(如obj.name)

- name
- length

## 1.2.arguments(Arrgy-like类似数组)转数组

- 所有传入的参数都在arguments这个里面

- 三种方式
  - 先(for xx of argument)再xx.push
  - [].splice.apply(arguments)/Array.Prototype.slice.apply(arguments):1,用apply使splice的this->arguments,2,用[]或Array.Prototype因为这样才能调用splic方法
  - ecma6:Array.from(arguments)/[...arguments]
- 箭头函数没arguments

## 1.3.剩余reset参数(是个数组)

- function foo(..arg) {console.log(arg)
- 注意去呗arg和arguments的区别

## 1.4.纯函数的使用

- 概念
  - 确定的输入一定产生确定的输出(不能被外部变量影响或影响外部变量)
  - 执行过程中不能副作用(如"触发事件")
- 实际开放中应用
  - react/reduc
- 没有必要每个函数都是纯函数
- 例子
  - slice:slice截取数组,不会对原数组进行任何操作,而是生成一个新数组(纯函数)
  - splice:截取,返回一个新数组,但会对原数组修改(不是纯函数)
- 优势
  - 放心的写:不用担心会被外面的值其状态改变而影响
  - 放心的用:不用担心会影响这个引用其他地方的方法会影响我当前的代码

## 1.5.柯里化(currying)的过程

- 把函数function(x,y,z){} -> var x = y =>z => 的过程叫currying 

  - 调用转变:fn(x,y,z)=>fn(x)(y)(z)

- 柯里化函数

  - ```javascript
    function fn(x) {
        return function(y) {
            return function(z) {
                return x+y+z
            }
        }
    }
    fn(2)(4)(6)
    ```

  - ```javascript
    var add = x => y => z => x+y+z(箭头函数有作用域链,可访问外部变量)
    add(2)(4)(6)
    由于从根部可达性(var add=)会导致内存泄漏,所以
    var add = null
    上面函数要是var add2 = fn()则也要var add2 = null
    ```

- 案例与自动柯里化的函数封装

  ```javascript
  function logInfo(date,type,message) {
      
  }
  自动柯里化的函数封装
  function hyCurrying(fn) {
     function curryFn(...args) {
         if(args.length >= fn.length) {
             //return fn(...args) 没考虑this
             return fn.apply(this,args)
         } else {
             return function(...newArgs) {
                 //return curryFn(...args.concat(newArgs))没考虑this
                 return currryFn.apply(this,args.concat(nesArgs))
             }
         }
     }
      return curryFn
  }
  var log = hyCurrying(logInfo)
  log(xx)(xxx)(xxx)
  ```

  

- 优势

  - 拆分为职责单一,方便调试
  - 参数复用

## 1.6 组合函数的使用

- 多个函数组合在一起,以此调用

- 封装组合函数

  - ```JavaScript
    function double(x) {
        return x*2
    }
    fucntion pow(x) {
        return x**2
    }
    function composeFN(...fns) {
        // 1, 边界判断(edge case)
        var length = fns.length
        if (length <= 0) return
        for(var i = 0; i < lenghth; i++) {
            var fn = fns[i]
            if (typeof fn !== "function") {
                throw new Error(`index position ${i} must be function`)
            }
        }
        
        // 2. 返回的新函数
        return function(...args) {
            var result = fns[0].apply(this, args)
            for (var i = 1; i < length; i++) {
                var fn = fns[i]
                result = fn.apply(this, [result])//这里的this指向newFN(xx).apply(obj)()的obj
            }
            return result
        }
    }
    var newFN = composeFn(double, pow, console.log)
    newFn(100)//直接会有结果,而不用console.log(newFn(100))
    //但使用newFn会报错,因为reusult没有结果(console.log函数没有返回值)
    ```

# 二.额外知识

## 2.1 with

```javas
var a = 3
with (a) {
	console.log(a)//即给其加了个作用域,可访问到a
}
```

## 2.2. eval

```
var a = `var b = 2;console.log(b);`
eval(a)//2,将字符串当作代码执行
存在安全隐患和性能问题。
尽量避免使用 eval
```

## 2.3.严格模式

- JavaScript当你出现语法错误,他不会指出错误而是依旧继续编译,而开启严格模式会指出你的错误

- 开启("use strict")

  - 在文档开头,则全文档是严格模式
  - 某个函数里面function() {"use strict"}

- 限制了什么

  - 无法意外创建全局变量

  - 严格模式会引起静默失败(silently fail)的赋值也抛出异常

    - ```javas
      var obj = {
      	name : 'cao'
      }
      Object.difineProperties(obj,"name",{
      	configurable : false//即不能修改obj的那么
      	wirtable: false
      })
      obj.name = "wang"//严格模式下会报错,非严格模式则静默失败(silently fail)
      delete obj.name//报错
      ```

  - 试图删除不可删除的属性会报错(即上面的writable)

  - 不允许函数参数有相同的名称

  - 不允许0的八进制语法,即console.log(0x)

  - 不可with

  - eval不会为上层引用变量

    - ```javas
      eval(`var message = 2`)
      consolo.log(message)//undefined
      ```

  - this绑定不会默认转为对象

    - ```javas
      function foo() {
      	console.log(this)
      }
      foo.apply(123)//非严格是string {123}一个对象,严格下是123
      ```

# 三.对象增强

## 3.1.属性描述符

	- 数据属性
	- 存储属性

## 3.2 数据属性描述符(默认全为true)

- configurale
- enumerable(不可迭代,不可使用for in/Object.keys())
- writable
- value(自定义返回什么)

## 3.3 存储属性描述符

- configurable

- enumerable

- set

  - ```javas
    当obj.name = 'xx'就会触发get方法
    ```

- get

  - 当console.log('xxx')就会触发set方法

## 3.4 定义多个属性描述符

```JavaScript
Object.defineProperties(obj,{
    "name" :{
        configu...
    },
    "age": {
        
    }
})
```

## 3.5 对象方法的补充

- 获取对象的属性描述符
  - getOwnPropertyDescriptor
  - getOwnPropertyDescriptors
- 禁止对象扩展新属性:prevenExtensions : Object.preventExtensions(obj1)
  - 给对象加个新的属性就会失败(严格模式会报错)
- 密封对象,不允许配置和删除属性:seal : Object.seal(obj1)
  - 实际是调用preventExtensios
  - 并且将现有属性的configurable:false
- 冻结对象,不允许修改现有属性 : freeze : Object.freeze(obj1)
  - 实际上是调用seal
  - 并且writable:false